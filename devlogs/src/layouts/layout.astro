---
import Navbar from "../components/navbar.astro";

export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
        <meta name="title" content={title} />
        <!-- og -->
        <meta property="og:type" content="blog" /><meta
            property="og:url"
            content="https://project-omikron.chazzox.uk"
        />
        <meta property="og:title" content="Project Omikron - The Devlogs" /><meta
            property="og:description"
            content="Attempting to write a blog and a game at the same time :)"
        />
        <!-- twitter -->
        <meta property="twitter:title" content="Project Omikron - The Devlogs" />
        <meta
            property="twitter:description"
            content="Attempting to write a blog and a game at the same time :)"
        />
    </head>

    <body class="flex min-h-screen flex-col bg-black text-white">
        <Navbar />
        <main class="flex-1">
            <slot />
        </main>
    </body>
</html>
